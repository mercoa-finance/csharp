using Mercoa.Client;

#nullable enable

namespace Mercoa.Client;

public class EntityGroupTypesClient
{
    private RawClient _client;

    public EntityGroupTypesClient(RawClient client)
    {
        _client = client;
    }
}
